# JarvisOS GRUB theme — sci-fi HUD. The atmosphere (wordmark, grid, the
# instrument rings, captions, key legend) is baked into background.png,
# rendered from background.svg at build time. GRUB only draws the two
# LIVE things on top: the OS list and the countdown bar. All positions are
# percentages so they track the background at any boot resolution (the
# image is stretched to fill). Palette matches the HUD: ground #090D12,
# ember #F0714A, text #E6ECF0 / muted #9BAAB4 / faint #64747F.
#
# Font names below are the pf2 internal names grub-mkfont assigns in
# default.nix (jbitem / jbsel / jbhint), NOT file names.

title-text: ""
desktop-image: "background.png"
desktop-image-scale-method: "stretch"
desktop-color: "#090D12"
terminal-font: "jbhint Regular 18"
terminal-left: "0"
terminal-top: "0"

# --- the OS chooser (left column, aligned to the baked layout) -------
+ boot_menu {
    left                = "7.7%"
    top                 = "46%"
    width               = "46%"
    height              = "30%"
    item_font           = "jbitem Regular 30"
    item_color          = "#9BAAB4"
    selected_item_font  = "jbsel Bold 30"
    selected_item_color = "#F0714A"
    item_height         = "58"
    item_padding        = "6"
    item_spacing        = "16"
    menu_pixmap_style   = ""
}

# --- countdown: the instrument ticks itself down ---------------------
# The one thing GRUB can actually animate. Ember ticks sit on the r≈300
# ring of the baked instrument and disappear one per second as the
# timeout runs out. Box is centred on the reticle (bg: 77.7% x, 40.8% y)
# and square in real pixels (640x640) so the ticks form a true circle.
+ circular_progress {
    id             = "__timeout__"
    left           = "65.2%"
    top            = "18.6%"
    width          = "25%"
    height         = "44.4%"
    num_ticks      = "48"
    ticks_disappear = "true"
    start_angle    = "-90"
    center_bitmap  = "hub.png"
    tick_bitmap    = "tick.png"
}

# live seconds, under the instrument
+ label {
    id     = "__timeout__"
    left   = "60%"
    top    = "62%"
    width  = "35%"
    align  = "center"
    color  = "#F0714A"
    font   = "jbhint Regular 18"
    text   = "@TIMEOUT_NOTIFICATION_SHORT@"
}
