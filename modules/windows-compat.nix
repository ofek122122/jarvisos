# STUB — Phase 2 (blueprint §08).
#
# Will hold: boot.binfmt registration for PE binaries, wine-staging +
# Proton-GE via umu-launcher, per-app bubblewrap-confined prefixes managed
# by jv-compat, all screened by jv-guard first.
#
# Deliberately empty in Phase 0; it exists so the module layout is final.
{ ... }:
{
}
