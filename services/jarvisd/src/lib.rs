//! jarvisd — the JarvisOS bus broker (CLAUDE.md invariant 1: services
//! communicate ONLY via this bus) and the `jv` debug CLI's client library.

pub mod broker;
pub mod client;
pub mod proto;
pub mod schema;
pub mod time;
