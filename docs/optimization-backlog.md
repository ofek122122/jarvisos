# Optimization backlog — human-review-required

Generated by the multi-agent optimization pass (2026-09-23). These findings were
adversarially CONFIRMED as real but flagged human-review-required — they touch
jv-act, schemas, the bus protocol, boot/NVIDIA, the VRAM ladder, or otherwise
warrant a deliberate look. The 8 safe findings were already applied (see git log:
perf(python), perf(jarvisd), hosts(ares), and the fix(brain) bus-drop discovery).

Confirmed total: 38 | auto-applied: 8 | in this list: 27

## 1. [high impact / medium risk / resource] Quantized V-cache requested without flash-attention (rungs 1-3)
- **Where:** `services/jv-brain/jv_brain/launcher.py:92`
- **Change:** Add --flash-attn (or --flash-attn on) to llama_args for the GPU rungs, verified against the pinned llama-cpp build and VRAM checked with nvidia-smi before nixos-rebuild switch; add a launcher test asserting -fa accompanies any quantized V cache.
- **Why / verification:** REAL (code) with a build-dependent outcome. Verified: rungs 1-3 have kv_type q8_0 (config.py:61-63), so llama_args appends --cache-type-k q8_0 --cache-type-v q8_0 (launcher.py:92-93); grep confirms --flash-attn appears nowhere in jv-brain. llama.cpp requires flash attention for a quantized V cache — without it, older builds error at launch, aborting the ExecStart entirely, while very recent builds default --flash-attn=auto and may enable it silently. The pin is pkgs.llama-cpp (modules/jarvis-services.nix:17), version not otherwise fixed, so the actual behavior can only be settled against the built binary. If it silently keeps an f16 V cache, rung.kv_bytes() (config.py:51-55) still halves the KV budget, so pick_rung selects a rung whose real KV is ~2x assumed -> OOM on the 6 GB card, the exact failure the ladder prevents. human_review_required: this is the llama-server unit ExecStart and the VRAM ladder (CLAUDE.md #6 + NixOS build->test->switch); enabling -fa on Turing must be verified with nvidia-smi before switch. No test asserts -fa on the q8 rungs (test_launcher.py only checks the f16 rung omits --cache-type-k).

## 2. [high impact / medium risk / robustness] Partials re-transcribe the whole growing utterance every 0.7 s on the perception thread, risking mic-queue starvation and lost audio
- **Where:** `services/jv-ears/jv_ears/pipeline.py:196`
- **Change:** Transcribe partials on a bounded window or move ASR to its own worker so feed() never blocks pulling the mic; never re-transcribe the full growing buffer inline.
- **Why / verification:** REAL and accurate. Line 196-197 fires _emit_transcript('partial') every _partial_hop while gated, and _emit_transcript (line 130) synchronously runs asr.transcribe over the ENTIRE _utt_audio buffer from scratch each time — O(n^2) CPU. It runs inline in the single feed()/pipeline worker that also pulls from MicSource, whose queue is maxsize=64 with drop-newest on overflow (audio.py:69,77). Repeated full re-transcribes exceeding real time back the queue up and silently drop chunks, degrading the final transcript and stalling wake/VAD — a direct invariant-5 (nothing blocks the hot path) violation. Not auto-safe: the fix is a hot-path redesign (bounded incremental window or a dedicated ASR worker) affecting perception timing and transcript correctness — needs human review.

## 3. [high impact / medium risk / latency] Synthesis is serialized behind playback — each sentence's synth time is dead air in a streamed reply
- **Where:** `services/jv-voice/jv_voice/service.py:139`
- **Change:** Prefetch: synthesize the next queued item in the executor while the current item plays; discard the prefetched buffer if its reply_group is dropped or an urgent preempt/wake fires before it is played.
- **Why / verification:** CONFIRMED. _speak() (service.py:82-96) runs synth in the executor THEN calls player.play in one task; run() (137-140) only creates the next _speak task after speak_task.done(), i.e. after N's synth AND playback both finish. So sentence N+1's Piper synth does not begin until N's playback completes, inserting synth latency as silence between every streamed sentence — real, and it defeats the streaming overlap jv-brain sends via reply_group. Whisper/Piper are CPU-only (invariant 6), so a spare-core prefetch is sound in principle. NOT auto-safe: the fix must correctly interact with _drop_group/_abort on wake+preempt (barge-in, BRIEF exit item 3) and the urgent-preempt path (_enqueue 55-59) — a prefetched buffer for a dropped reply_group must be discarded, and no existing test covers prefetch. Concurrency + interruption semantics = human review.

## 4. [high impact / low risk / correctness] Context budget is fixed at rung-0 size; overflows the 2k-ctx fallback rungs
- **Where:** `services/jv-brain/jv_brain/config.py:97`
- **Change:** Derive max_context_chars/trim_target_chars from the active rung's ctx (via _rung()/LADDER) minus system+tool-def+max_tokens headroom, so trimming keeps the prompt inside the 2048-ctx fallback rungs.
- **Why / verification:** REAL. Verified: max_context_chars=12_000 and trim_target_chars=7_000 are module constants (config.py:97,102) used unconditionally by Conversation._trim/_chars (service.py:127,141); nothing reads the active rung to size the window. Rungs 2/3 launch llama-server with --ctx-size 2048 (config.py:62-63 -> launcher.py:75-76). The prompt sent is system(~1415 tok, service.py:278) + up to ~3000 tok of trimmed convo (12k chars) + tool defs + 320 max_tokens generation — far over 2048. _rung() (service.py:184) already reads the rung for health but is never used for sizing. Under the exact VRAM pressure the ladder exists for, this forces llama.cpp context-shift/eviction of the oldest tokens (the system prompt + warmed cache). human_review_required: touches the VRAM-ladder degraded-path correctness (CLAUDE.md #6); no test covers rung-derived context sizing (test_conversation.py tests only fixed-size trims).

## 5. [high impact / low risk / robustness] Tool loop can spin forever once the per-turn call limit is hit
- **Where:** `services/jv-brain/jv_brain/service.py:339`
- **Change:** Add a max-rounds counter to the while loop; on the final round send tool_choice='none' (or break and return the accumulated text) so a stuck model is forced to a plain answer instead of looping.
- **Why / verification:** REAL. Verified: _respond's `while True` (service.py:339) only returns when the model emits no tool_calls (line 341). calls_used caps EXECUTIONS at MAX_TOOL_CALLS_PER_TURN=5 (line 353), but past the cap each call is answered with a 'tool_call_limit' tool-role error (lines 353-357) and the loop re-enters _stream_turn — there is no round cap. A model that keeps emitting tool_calls in reply to those errors loops indefinitely, each pass a full generation on the single --parallel 1 slot. Because _input_worker processes turns serially (service.py:602-604), this wedges the conversation and pins GPU/CPU while on_sentence stays suppressed (line 258, tool_frags present) — the user hears nothing. Real starvation of the single input worker (CLAUDE.md #5). human_review_required: the fix changes the tool-calling control flow / payload (tool_choice) of a voice-facing loop and no existing test exercises the runaway-loop scenario.

## 6. [medium impact / medium risk / latency] Final transcribe (~2.2 s) blocks continuous wake+VAD during ASR
- **Where:** `services/jv-ears/jv_ears/pipeline.py:122`
- **Change:** Run asr.transcribe on a dedicated ASR worker (handed a copy of _utt_audio) so feed() keeps servicing wake/VAD/mic while ASR runs.
- **Why / verification:** REAL. _on_speech_end (line 113) calls _emit_transcript('final') synchronously at line 122, which runs asr.transcribe (line 130) inline in the single worker thread; for the transcribe duration wake.feed()/vad.feed() stop and the MicSource queue backs up (same drop-newest starvation). This is the invariant-5 hot-path-stall the finding claims, and matches the PHASE1-STATUS STILL-OPEN 'ASR ~2.2s fixed, runs after speech_end' item whose fix is explicitly a bigger own-session change. Caveat: the 'barge-in is dead' framing is loose — jv-voice is not yet speaking during post-utterance ASR, so this stalls wake re-arm and mic intake rather than true speech barge-in; the core blocking claim stands. Not auto-safe: design change to the perception thread — human review.

## 7. [medium impact / medium risk / latency] Up to 100 ms of dead air between sentences: loop doesn't wake on speak_task completion
- **Where:** `services/jv-voice/jv_voice/service.py:142`
- **Change:** Race next_frame() against speak_task completion (asyncio.wait FIRST_COMPLETED) so a finished utterance starts the next immediately; ensure a not-yet-completed next_frame() is retained, not dropped, across iterations.
- **Why / verification:** CONFIRMED. The run loop parks on `await asyncio.wait_for(self.bus.next_frame(), timeout=0.1)` (142-143); when a speak_task completes during that wait, the loop only re-checks the queue after the 0.1 s timeout (or an unrelated frame), adding up to 100 ms of silence per sentence boundary. Real. NOT auto-safe despite the low-risk label: replacing the wait_for with asyncio.wait(FIRST_COMPLETED) over next_frame() + speak_task is a correctness-sensitive change to the single bus-consumption loop — a cancelled/pending next_frame() coroutine must not silently drop an incoming speech.say/audio.wake frame (invariant 5: nothing blocks/loses bus traffic). Needs human review of the frame-preservation semantics.

## 8. [medium impact / medium risk / latency] 50 ms polling loop delays barge-in stop and completion detection
- **Where:** `services/jv-voice/jv_voice/player.py:40`
- **Change:** Drive completion off sd.OutputStream/sd.play finished_callback setting an asyncio.Event (via loop.call_soon_threadsafe) and await it against abort; keep the len(audio)/rate + 10 s deadline as a stuck-device backstop.
- **Why / verification:** CONFIRMED. SoundDevicePlayer.play (player.py:34-43) polls abort.is_set() and sd.get_stream().active every 50 ms (line 40), so a wake/preempt abort and normal completion are each detected up to 50 ms late — real added latency on the barge-in path (BRIEF exit item 3) and inter-sentence gap. NOT auto-safe: this is the real-hardware playback path (PortAudio->PipeWire), marked `# pragma: no cover` with no test coverage (tests use FakePlayer). The finished_callback/asyncio.Event rewrite must preserve the existing hard-won behavior — completion decided by the stream not by duration arithmetic, and the +10 s deadline as a stuck-device backstop (comment 28-32, references the 2026-09-16 tail-clipping field bug). Hardware timing + no tests = human review.

## 9. [medium impact / medium risk / performance] Broker fully decodes then re-encodes every published frame's body
- **Where:** `services/jarvisd/src/broker.rs:166`
- **Change:** Parse only the envelope header for routing and forward the client's original frame bytes verbatim inside ServerMsg::Frame, instead of decode-to-Value-then-re-encode.
- **Why / verification:** CONFIRMED. read_msg (proto.rs:45-61) deserializes the whole ClientMsg via rmp_serde::from_slice; for Pub{frame} that means the entire envelope body (rmpv::Value, including nested transcript word arrays) is materialized. handle_conn only calls validate_envelope (proto.rs:96) which reads scalar envelope fields and body.is_map(), never inspecting body contents. publish_value (broker.rs:166) then re-serializes the whole Value via proto::encode -> rmp_serde::to_vec_named. So each routed frame pays a full decode + full re-encode on the single central broker. Real per-frame cost on the hot path shared with CPU-Whisper/brain. NOT auto-applicable: the fix requires msgpack byte-surgery (extracting the client's raw frame-value slice out of the Pub message and splicing it into a ServerMsg::Frame wrapper) and a bytes-based partial envelope validator. It changes what subscribers actually receive (today they get a normalized re-encoding; verbatim forwarding is byte-for-byte different, e.g. integer compaction/map ordering may differ from the current output), which is bus routing behavior all services depend on. Central broker + frame emission behavior -> human review.

## 10. [medium impact / medium risk / latency] jv-voice/jv-ears have no CPU scheduling weight — interactive audio loses to the brain under load
- **Where:** `modules/jarvis-services.nix:204`
- **Change:** Give jv-voice (and jv-ears) a modest serviceConfig.CPUWeight boost so the interactive audio path is not starved by brain/Whisper.
- **Why / verification:** REAL as an observation. Confirmed jv-ears (186-202) and jv-voice (204-217) have serviceConfig with only ExecStart/Restart/RestartSec — no CPUWeight or Nice. audio.nix:5 grants rtkit RT priority to the PipeWire graph, but the Python ASR/Piper TTS processes are not in that graph, so they run at default weight and compete with the 8B-Q4 brain prefill/decode + CPU Whisper on 6c/12t. This does NOT touch the bus (invariant 1 intact) or jv-act. But it is a scheduling-balance tuning change: the finding itself rates risk medium and warns over-boosting starves jv-brain/jv-llm and perturbs the VRAM/brain latency balance (invariants 5+6). A value must be tuned and measured against the latency budget, not applied blind — not a clean-cut auto-apply. Human review + measurement required.

## 11. [medium impact / low risk / robustness] _input_worker has no error isolation — one unhandled exception kills all future turns
- **Where:** `services/jv-brain/jv_brain/service.py:598`
- **Change:** Wrap the worker loop body in try/except; on an unexpected exception publish a degraded sys.health with the error and continue to the next queued turn instead of letting the task die.
- **Why / verification:** REAL. Verified: _input_worker's loop body is a bare `await self._handle_input(*args)` (service.py:602-604) with no try/except. _handle_input only guards _respond against (httpx.HTTPError, KeyError, ValueError) (service.py:525); anything else — e.g. profile.save() OSError (lines 458,514), an onboarding TypeError, or a JSON error outside the guarded block — propagates and permanently terminates the worker task. run()'s frame loop keeps calling inputs.put_nowait (lines 629,639), so the queue grows while the user gets zero replies and no crash surfaces: a silent dead brain. A voice-facing service should degrade one turn, not the whole assistant. No invariant is broken by the current code. human_review_required: no test exercises _input_worker resilience, and the proposed fix publishes a sys.health frame (verify field shape against the health consumers) — a quick human glance plus a regression test is warranted before applying.

## 12. [medium impact / low risk / correctness] distil-small.en cannot serve the 'Whisper may receive Hebrew' requirement
- **Where:** `services/jv-ears/jv_ears/config.py:69`
- **Change:** Human decision: either explicitly document Hebrew input as out-of-scope for v0 (and gate/annotate confidence when detected lang != en), or swap to a multilingual model with the fetch pins and latency budget re-verified.
- **Why / verification:** REAL. whisper_dir (line 69) points at faster-distil-small.en, an English-only model (also SHA256-pinned to Systran/faster-distil-whisper-small.en in models/fetch.sh:46-50). CLAUDE.md's locked decision is 'English voice in/out (Whisper may receive Hebrew; replies in English)', and PHASE1-STATUS still lists the Hebrew transcript fixture as an open TODO(machine). An .en model will hallucinate English for Hebrew and asr.py reports a normal exp(avg_logprob) confidence with no out-of-language signal, so this is a genuine requirements gap. Not auto-safe and human-review-required: resolving it is a product decision (defer Hebrew to v0 vs swap the model) that changes SHA256-pinned model fetching. Note the change_summary's 'multilingual distil variant' is largely inaccurate — distil-whisper small is English-only; a multilingual swap means the heavier non-distil whisper-small, which affects the CPU/latency budget and must be weighed by a human.

## 13. [medium impact / low risk / performance] Audit append re-opens file and create_dir_all's per entry with blocking I/O in async task
- **Where:** `services/jv-act/src/audit.rs:60`
- **Change:** Open the audit file once in append mode at startup and reuse the handle; do create_dir_all once; run blocking I/O via spawn_blocking or a dedicated writer task. jv-act change - human review required.
- **Why / verification:** CONFIRMED. AuditLog::append (audit.rs:59-69) calls std::fs::create_dir_all + OpenOptions::open + writeln! + implicit close on EVERY entry, synchronously. It is invoked from within tokio::spawn tasks (service.rs spawn_request via audit.append at lines 248,367,389,416), so these blocking std::fs calls run on tokio worker threads. The critical pre-execution intent line (service.rs:367) that gates whether the machine is touched sits on the action-latency path, so this taxes voice/gesture-to-effect time. jv-act code (invariant 3: only jv-act mutates state, all its code is human-reviewed) -> ALWAYS human review, never auto-apply. Additional subtlety a reviewer must handle: multiple concurrent spawn_request tasks currently rely on O_APPEND atomic appends via independent handles; sharing one handle would need a Mutex to preserve line integrity.

## 14. [medium impact / low risk / performance] nvidia-smi is forked once per second, forever, for the VRAM field
- **Where:** `services/jv-context/jv_context/system.py:44`
- **Change:** Real cost. Fix options (NVML via pynvml, or decoupling the VRAM probe to a coarser cadence than the 1 Hz snapshot) both touch the VRAM ladder and/or the flake — route to human review; do not auto-apply.
- **Why / verification:** CONFIRMED. gpu_vram_free_mb() (system.py:42-54) shells out to `nvidia-smi` via subprocess.run; snapshot() calls it every cycle (line 74) and _pump_system loops at system_period_s=1.0 (config.py:35), so ~86k nvidia-smi forks/day. Note the probe DOES run in run_in_executor (service.py:71), so it does not block the bus — the finding correctly cites invariant 6, not 5. BUT this is the VRAM probe that feeds the brain load/unload ladder (invariant 6, explicitly called out in the task as human-review-gated). Changing its cadence to 5 s alters how fast the scheduler sees a game-launch VRAM spike; switching to pynvml/NVML adds a runtime dependency that must be declared in the flake (NixOS discipline). Both are deliberate scheduling/packaging decisions, not auto-applyable.

## 15. [medium impact / low risk / correctness] hardware.graphics.enable32Bit missing — 32-bit Proton/Wine games get no GL/Vulkan
- **Where:** `modules/gpu-nvidia.nix:10`
- **Change:** Add hardware.graphics.enable32Bit = true; so 32-bit Proton/Wine titles get NVIDIA GL/Vulkan.
- **Why / verification:** REAL. Confirmed gpu-nvidia.nix:10 sets only hardware.graphics.enable=true; grep finds enable32Bit nowhere. Supporting facts check out: audio.nix:10 sets alsa.support32Bit, and windows-compat.nix ships wineWowPackages.stagingFull (lines 12,24) + umu-launcher/Proton-GE (line 27). So the design commits to Windows games and invariant 6 treats game launch as a real VRAM state; 32-bit titles need 32-bit NVIDIA GL/Vulkan libs, absent without enable32Bit. The change is additive and does not affect the 64-bit brain/vision path, but it lives in the NVIDIA/GPU driver module and pulls in the 32-bit driver library set — the rubric flags anything touching NVIDIA / the game-launch VRAM state for human review. Correct finding, but review + on-hardware verify required.

## 16. [low impact / medium risk / resource] Entire effects chain runs in float64, doubling CPU and memory on the hot path
- **Where:** `services/jv-voice/jv_voice/chain.py:48`
- **Change:** Run the chain in float32 (audit the reverb wet/dry normalization ratio for precision), and confirm test_chain's peak<=0.951 and monotonic-intensity assertions still hold plus a listening check before applying.
- **Why / verification:** CONFIRMED as fact: apply_chain upcasts to float64 (chain.py:48) and runs highpass/shimmer/fftconvolve/tanh in double precision, casting back to float32 (line 70). float32 would roughly halve DSP cost. NOT auto-safe: this is the versioned signature voice chain (invariant 9 — voice identity is deliberate, human-applied), which Ofek auditions from harness fixtures. float32 rounding subtly changes the output waveform, and test_chain.py asserts an exact peak bound (<=0.951) and RMS-delta monotonicity that a precision change could perturb (esp. the reverb-normalization ratio the finding flags). Impact is only 'low' on this CPU/32 GB box, so the small win does not justify auto-touching a quality-sensitive, versioned-identity path. Human review + re-audition.

## 17. [low impact / medium risk / robustness] NVIDIA driver not truly pinned — 'production' floats with flake.lock
- **Where:** `modules/gpu-nvidia.nix:23`
- **Change:** Replace nvidiaPackages.production with an explicit mkDriver pin to 595.91.07 in its own commit, per the recorded running version.
- **Why / verification:** REAL. Confirmed gpu-nvidia.nix:23 package = config.boot.kernelPackages.nvidiaPackages.production, and the comment (lines 13-22) explicitly acknowledges 'the effective pin is nixpkgs' lock entry' with running version 595.91.07 recorded. So a nixpkgs bump silently moves the GPU driver, contrary to CLAUDE.md NixOS discipline ('Pin the NVIDIA driver version explicitly. Change it deliberately, alone, in its own commit'). This is squarely the flagged NVIDIA + boot-critical area; mkDriver hard-pinning needs correct sha256 hashes and must build against the current kernel, and must be its own tested, boot-verified commit. Cannot be auto-applied (hashes unknown) and must not be bundled. Human review required.

## 18. [low impact / low risk / latency] LLM name-extraction fallback evicts the pinned prompt-cache slot
- **Where:** `services/jv-brain/jv_brain/service.py:467`
- **Change:** Preserve slot 0's warmed prefix during onboarding — e.g. perform name extraction as a normal _payload turn sharing the pinned system prefix, or explicitly accept/skip the extraction cost; do not rely on a second id_slot under --parallel 1.
- **Why / verification:** REAL, low impact; proposed fix is partly infeasible. Verified: _llm_extract_name POSTs a hand-built payload (service.py:470-483) with a DIFFERENT system prompt ('Extract only the person's name...') and no id_slot/cache_prompt, unlike _payload (service.py:196-208) which pins id_slot:0 + cache_prompt:true. With --parallel 1 (launcher.py:87-88) slot 0 is the only slot and the one _warmup pinned (service.py:277-296), so this differing-prefix request re-prefills and evicts the warmed system-prompt KV; the first real exchange then re-pays the ~10.5s prefill. Only fires during first-boot onboarding when rule-based extract_name misses, so impact is bounded to one path. human_review_required: the change_summary's 'route to a distinct id_slot' is not possible under --parallel 1 (no second slot), and cache_prompt=false would not prevent the eviction — the correct remedy (e.g. run extraction with the SAME system-prompt prefix, or accept the one-time cost) needs deliberate design, not an auto-apply.

## 19. [low impact / low risk / robustness] Unbounded input queue lets stale voice turns pile up behind a slow reply
- **Where:** `services/jv-brain/jv_brain/service.py:629`
- **Change:** Bound the queue or coalesce/drop older 'voice' turns when a newer final transcript arrives, so the brain answers the current utterance rather than a backlog — decided with a human given the UX tradeoff.
- **Why / verification:** REAL mechanism, but a product/UX design decision rather than a clear bug. Verified: inputs is an unbounded asyncio.Queue() (service.py:610) fed by put_nowait from the frame loop (service.py:629,639) and drained one-at-a-time by the serial _input_worker (service.py:602-604). A stalled turn (a tool waiting the full ACTION_RESULT_TIMEOUT_S=60s at service.py:316, or a slow CPU-rung generation) lets later utterances queue and be answered long after they are relevant. Whether to bound/coalesce/drop stale voice turns is a behavioral choice affecting the voice UX (and could drop legitimate turns), so it must not be auto-applied. human_review_required: changes user-facing voice behavior and interacts with the follow-up/onboarding turn ordering.

## 20. [low impact / low risk / simplify] Synthesis round-trips audio through an in-memory WAV encode/decode
- **Where:** `services/jv-voice/jv_voice/tts.py:24`
- **Change:** Concatenate int16 arrays from voice.synthesize(text) chunks directly, reading the sample rate from the chunk/voice config, dropping the wave.open BytesIO round-trip — verified against the installed piper-tts 1.3 AudioChunk API.
- **Why / verification:** CONFIRMED as fact: synth (tts.py:22-32) writes synthesize_wav into a BytesIO then re-opens the WAV to recover int16 PCM via frombuffer — a real, avoidable encode/parse round-trip on the per-sentence path. piper-tts>=1.3 is indeed the pinned dep (pyproject.toml), and 1.3 does expose a chunk-yielding synthesize() API. NOT auto-safe here: the exact AudioChunk surface (int16 array attribute name, sample-rate source) cannot be verified in this environment (piper model + runtime absent), and there is no test exercising the real synth() path (tests stub the synthesizer). A wrong attribute or rate source would break synthesis silently. Correct but unverifiable-in-place dependency-API change => human review/verification against real piper.

## 21. [low impact / low risk / latency] Writer issues one socket write syscall per frame; no coalescing
- **Where:** `services/jarvisd/src/broker.rs:294`
- **Change:** After pop(), drain remaining queued items non-blocking and emit them in one buffered/vectored write_all, preserving order and drop-oldest semantics.
- **Why / verification:** CONFIRMED. The writer task (broker.rs:293-304) does queue.pop().await -> one w.write_all per item; OutQueue::pop (line 113) returns exactly one QItem. Under streaming bursts (Whisper partials) multiple small frames are already queued yet each is its own write syscall. The coalescing idea is valid. NOT flagged auto-safe though: it is not a purely mechanical swap. It requires a new non-blocking drain path on OutQueue and building a combined buffer while preserving Frame/Ctl ordering and the existing drop-oldest accounting; large-burst buffering and partial-write edges need care. It sits on the central broker write path all bus traffic funnels through (invariant 1) and interacts with the drop-oldest semantics validated by slow_consumer_drop_oldest. Warrants review.

## 22. [low impact / low risk / resource] read_msg allocates and zero-fills a fresh buffer per frame
- **Where:** `services/jarvisd/src/proto.rs:58`
- **Change:** Add a read_msg variant that reads into a caller-owned reusable Vec (resize, no zero-init) held by the connection read loop.
- **Why / verification:** CONFIRMED. proto.rs:58 does `let mut buf = vec![0u8; n as usize]` then read_exact overwrites the whole buffer (line 59) — the zero-fill is pure waste and each frame mallocs/frees on the broker's central read path. A per-connection reusable scratch buffer (resize, no zero-init) removes both. NOT flagged auto-safe: read_msg is the shared protocol read primitive used by the broker read loop AND every BusClient (client.rs); the fix changes its signature or adds a variant threading a caller-owned buffer, plus wiring it into the read loop, touching the central proto module and the hot read path all consumers depend on. Low risk but not purely local -> review. (The bare zero-fill removal alone would be trivial, but the proposed reusable-buffer refactor is an API change.)

## 23. [low impact / low risk / performance] Every incoming frame body is deep-converted rmpv -> serde_json::Value to read a couple of fields
- **Where:** `services/jv-act/src/service.rs:173`
- **Change:** Read kind/text/request_id directly from the rmpv body via the existing get() helper; only convert to serde_json where the full args object is needed. jv-act change - human review required.
- **Why / verification:** CONFIRMED. body_json (service.rs:94-98) runs serde_json::to_value on the full body of every subscribed frame: intent.action (line 152), action.confirm (line 156), audio.transcript (line 174). For audio.transcript finals carrying a words[] array with per-word timings, this materializes the entire array into a serde_json tree only to read `kind` and `text`. jv-act already has direct rmpv accessors get()/as_f64() (lines 81-92) usable for these small reads. jv-act code (invariant 3) -> always human review, never auto-apply. A reviewer must keep the full serde_json conversion where the whole args object is genuinely consumed (intent.action -> spawn_request/executor).

## 24. [low impact / low risk / simplify] classify_answer runs (with String allocations) on every final transcript even when no confirm is pending
- **Where:** `services/jv-act/src/service.rs:180`
- **Change:** Short-circuit: only run classify_answer when a confirm is actually pending (check before classifying, or classify inside resolve_voice after the pending lock). jv-act change - human review required.
- **Why / verification:** CONFIRMED. On every audio.transcript final, service.rs:180 calls classify_answer(text) unconditionally, before any pending check; classify_answer (lines 49-63) does to_lowercase + filtered char collect + split_whitespace/join (multiple allocations). resolve_voice (line 203) only then locks pending and finds a matching window, so in the overwhelmingly common case (no confirm open) the classification work and allocations are wasted. jv-act code (invariant 3) -> always human review, never auto-apply. Reviewer note: a cheap pending-non-empty gate itself requires acquiring the async pending Mutex, so the cleanest form is to move classify_answer inside resolve_voice after confirming the pending map is non-empty.

## 25. [low impact / low risk / performance] from_body() calls typing.get_type_hints on every decode with no cache
- **Where:** `services/pylib/jarvis_bus/schema.py:264`
- **Change:** Add lru_cache(maxsize=None) around the get_type_hints lookup, edited in tools/gen_bindings.py and applied by regenerating schema.py from schemas/*.json — its own commit. Do not hand-edit the generated file.
- **Why / verification:** CONFIRMED as written, with a scope caveat. Line 264 is `hints = typing.get_type_hints(cls)`, re-resolving every annotation on each decode; hints are static per class and cacheable. The file header (line 1) is 'GENERATED by tools/gen_bindings.py ... DO NOT EDIT', and gen_bindings.py exists and contains the same from_body body (its line 251-268) — so per invariant 2 the fix must be made in the generator and regenerated as its own reviewed commit, never by hand-editing schema.py. Hand-editing a generated file is exactly what auto-apply would do, so safe_to_auto_apply=false / human_review_required=true. SCOPE NOTE: the runtime-hot-path impact is currently speculative — grep shows from_body is used only in tests (test_context.py, test_client.py), not in any live service decode path, matching the finding's own 'if/when services adopt it' hedge. Impact is correctly rated low.

## 26. [low impact / low risk / performance] _send concatenates the 4-byte header with the whole body before writing
- **Where:** `services/pylib/jarvis_bus/client.py:68`
- **Change:** Emit two writes before draining: `self._w.write(len(body).to_bytes(4,'big')); self._w.write(body); await self._w.drain()`. Wire format identical; route through human review because it is shared bus transport.
- **Why / verification:** CONFIRMED. Line 68 is `self._w.write(len(body).to_bytes(4, 'big') + body)` — the `+` allocates a fresh bytes object copying the entire payload on every publish, up to the 16 MiB MAX_FRAME cap, on the shared client every producer uses. Two writes (header, then body) with no await between them and a single drain() afterward produce byte-identical wire output — the length-prefixed-MessagePack wire format is unchanged, so this is not a protocol change. However, this is the shared bus transport that every service depends on; the task instructions gate 'the bus protocol' / core transport behind human review, and the perf benefit is marginal (impact low). A subtle regression here breaks all producers, so I default to review rather than auto-apply despite the change being byte-equivalent.

## 27. [low impact / low risk / simplify] Top-level pkgs instantiated separately from the system's own nixpkgs
- **Where:** `flake.nix:15`
- **Change:** Consider deriving packages.* from the system's pkgs (or sharing one nixpkgs config) to avoid a second instantiation and duplicated allowUnfree.
- **Why / verification:** Observation is REAL. Confirmed flake.nix:15-18 imports nixpkgs once with config.allowUnfree=true for packages.*, while nixosConfigurations.ares (lines 21-28) builds its own pkgs and default.nix:58 sets nixpkgs.config.allowUnfree=true again. So there are two nixpkgs evaluations and two allowUnfree surfaces. However this is not a defect: both configs are identical (allowUnfree=true) so there is no divergence in practice, and Nix caches evaluation, so the cost is minor eval hygiene — the finding itself calls it low priority 'for completeness'. The proposed fix is vague ('consider deriving packages.* from the system pkgs') and restructuring the flake package plumbing touches how jarvisd/jv-act (invariant 3, human-reviewed) and cuda packages build. Accurate but speculative and low-value; do not auto-apply, needs human judgment.

---

# Proposals from the Ralph loop (human review before any of this is built)

These are not findings from the 2026-09-23 pass. They are changes the
autonomous loop wanted to make and deliberately did not, because they touch a
guarded area (`schemas/**`, `jv-act`, boot/NVIDIA). Each one names the work it
is blocking.

## R1. [ui / §06] Nothing publishes AC-vs-battery or fullscreen, so two of §06's three "stop moving" rules cannot be honoured
- **Where:** `schemas/context.system.json`, `schemas/context.window.json` (frozen — invariant 2), publisher `jv-context`
- **Blocking:** PLAN A7 — `Motion.onBattery` and `Motion.fullscreen` exist as inputs with no source, pinned at `false`.
- **Change (proposed):** two additive, optional fields, each its own reviewed schema commit:
  - `context.system`: `on_battery` (boolean) — "power supply is discharging", read from `/sys/class/power_supply/*/status`. `battery_pct` already exists but says nothing about direction, and it is absent on ares, so its absence cannot be read as "on AC" either — a laptop that never publishes it and a desktop that has no battery look identical today.
  - `context.window`: `fullscreen` (boolean) — whether the focused window is fullscreen, which Niri already reports on its event stream. §06's rule is "full stop when a window is fullscreen", and the blueprint's stronger intent (the HUD yields entirely to fullscreen) needs the same bit.
- **Why it is worth a schema commit:** both are cheap for `jv-context`, which already polls the system at 1 Hz and already subscribes to the compositor event stream, and both have a second consumer beyond motion — a fullscreen bit is what lets the HUD get out of the way of a game or a film, and `on_battery` belongs in the same conversation as the VRAM ladder. Until they exist, "off on battery" is a rule the code is shaped for and cannot obey.
- **Not done autonomously because:** schemas are frozen law (GUARDRAILS: `schemas/**` is human-review-only). Nothing was worked around — the two inputs are real properties in `core/MotionPolicy.qml`, tested, and default to the value that is true on ares (a desktop, on AC). Wiring them later is one binding each in `Motion.qml`.
